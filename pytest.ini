[pytest]
markers =
    db: tests that require AlanStrats SQL Server connection
    polygon: tests that require POLYGON_API_KEY and live network
    slow: slow end-to-end tests (skip with -m "not slow")
testpaths = tests
