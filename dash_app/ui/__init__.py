"""
dash_app.ui — the Alan Trader design system.

Two modules:
  tokens     — design-system source of truth (colour, spacing, type, Plotly layout)
  components — shared, polished, presentation-only UI primitives

Usage:
    from dash_app.ui import tokens as D, components as C
"""
from dash_app.ui import tokens, components  # noqa: F401
